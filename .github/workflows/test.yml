name: CPS5002 Run Test

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: window-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.13


#      - name: Install dependencies
#        run:
#          python -m pip install --upgrade pip
#          pip install pytest

      - name: Run Tests
        run: python -m unittest discover -s test